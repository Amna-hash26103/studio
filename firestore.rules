rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // anyone logged in can read everything
    match /{document=**} {
      allow read: if request.auth != null;
      allow write: if false;  // disable writes
    }
  }
}
